import { FC } from "react";
export interface WorkspaceProps { }
const Workspace: FC<WorkspaceProps> = () => {
    return <main className="overflow-hidden">Workspace</main>;
};

export default Workspace;
